# SS Research — agent notes

## Next conversation priority

**Deploy live** using the plan in `docs/PLATFORM-AND-UX.md` and the checklist in `docs/NEXT-SESSION.md`:

- Vercel (frontend from this GitHub repo)
- Supabase (auth + Postgres + RLS, per-user research hubs)
- Seed default company template once per new hub — never auto-wipe user coverage

When the user returns without a specific ask, proactively offer to start that deploy work.
